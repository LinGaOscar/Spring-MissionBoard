# WBS 檢視分頁 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 專案詳情頁新增第三個分頁「WBS 檢視」，同一份 `tasks`/`task_categories` 資料以大類→子類→任務的樹狀結構呈現（含固定的「未歸類」節點），核心互動是拖曳任務改變所屬大類/子類；點任務列開與看板相同的編輯 modal。

**Architecture:** 純前端功能，全部使用既有、已測試過的後端 API（`GET .../tasks`、`GET .../task-categories`、`GET .../members`、`PUT .../tasks/{taskId}`），不動後端程式碼。新增 Vue 元件 `WbsView`，與現有 `KanbanView`/`AssignmentView` 平行、獨立 `mounted()` 時 fetch 自己的資料。實作前先把 `KanbanView` 內兩塊即將被 `WbsView` 共用的邏輯抽成 mixin/module 函式（比照現有 `toastMixin` 的抽法）：任務新增/編輯/刪除 modal（`taskModalMixin`）與 tasks/categories/members 三份資料載入＋寫入序列化佇列（`taskBoardMixin`），抽出後 `KanbanView` 行為不變（純重構）。根元件從雙分頁改三分頁（`v-if` 切換）。

**Tech Stack:** Vue 3（無 build 工具，vendored UMD），與看板／人員派工前端相同。

## Global Constraints

- 依 [`docs/superpowers/specs/2026-08-15-wbs-view-design.md`](../specs/2026-08-15-wbs-view-design.md) 執行。
- **不改後端**：拖曳改分類與 modal 存檔都重用既有 `PUT /api/projects/{id}/tasks/{taskId}`（body 含 `title`/`description`/`categoryId`/`priority`/`startDate`/`dueDate`），不新增端點。
- **分頁切換用 `v-if`，不用 `v-show`**（同人員派工分頁既有結論：避免多個分頁元件同時常駐掛載造成資料不同步）。
- **WBS 檢視不做分類的新增/改名/刪除/排序**——這些操作維持只在看板既有「分類管理」面板做，避免兩處維護同一份分類 CRUD 邏輯。
- **「未歸類」是固定虛擬節點**（`category_id IS NULL` 的任務），永遠顯示在樹狀最上方，即使沒有任務也顯示（顯示 `(0)` 與 `--`）。
- 分類節點（大類/子類/未歸類）旁的任務數／完成度**前端即時算**（用當下 `this.tasks` 陣列），不需新後端欄位；大類節點的彙總含其所有子類的任務。
- 同分類底下任務排序：依 `dueDate` 升冪，無到期日排最後，同值時依 `id` 升冪（沿用人員派工分頁既有規則，重用同一顆 `sortByDueDate` 函式，不重寫一份）。
- 拖曳任務改分類只在 `canWrite === true` 時可用（唯讀角色／封存專案不可拖曳），沿用看板既有 `draggable` pattern。
- v1 範圍外，明確不做：全域「全展開/全收合」按鈕、搜尋節點、樹狀列內直接編輯（負責人/日期/狀態不開 modal 改）、任務在同分類下的手動拖曳排序。
- 沿用既有命名慣例：CSS class 用字面 hex 色碼；`api()`/`showToast`/`taskWriteQueue` 錯誤處理與序列化寫入骨架照抄既有 `KanbanView` 寫法。

---

## Task 1: 抽出 `taskModalMixin`（純重構，看板行為不變）

**Files:**
- Modify: `src/main/resources/static/js/project-detail.js`

**Interfaces:**
- Consumes: 無新 API
- Produces: 新 mixin `taskModalMixin`（data: `modal`；methods: `emptyForm()`、`openCreate()`、`openEdit(t)`、`saveTask()`、`deleteTask()`），供 `KanbanView` 使用，Task 2 會讓 `WbsView` 也引入同一份

現況 `KanbanView` 的 `data()`（第 55-79 行）把 `modal` 跟其他看板專屬的 state（`columns`/`dragging`/`categoryPanelOpen`/preset 相關/`draggingCategoryId` 等）混在一起，`methods` 裡的 `emptyForm`/`openCreate`/`openEdit`/`saveTask`/`deleteTask` 也跟看板專屬的拖曳/分類 CRUD 方法混在一起。這次 `WbsView` 也要開同一顆任務編輯 modal，把這五個純粹跟「任務 modal」有關的 data/methods 抽出來，看板拖曳、分類管理維持原樣不動。

- [ ] **Step 1: 在 `toastMixin` 定義之後、`KanbanView` 定義之前，插入 `taskModalMixin`**

```javascript
  // KanbanView 與 WbsView 共用：任務新增/編輯/刪除的 modal 邏輯（歸類/指派/優先度/日期），
  // 兩個分頁都需要開同一顆 modal，抽出來避免各自維護一份
  const taskModalMixin = {
    data() {
      return {
        modal: {
          open: false, taskId: null,
          form: { title: '', description: '', assigneeId: null, categoryId: null, priority: null, startDate: null, dueDate: null },
        },
      };
    },
    methods: {
      emptyForm() {
        return { title: '', description: '', assigneeId: null, categoryId: null, priority: null, startDate: null, dueDate: null };
      },
      openCreate() {
        this.modal = { open: true, taskId: null, form: this.emptyForm() };
      },
      openEdit(t) {
        this.modal = {
          open: true, taskId: t.id,
          form: {
            title: t.title, description: t.description || '', assigneeId: t.assigneeId,
            categoryId: t.categoryId, priority: t.priority, startDate: t.startDate, dueDate: t.dueDate,
          },
        };
      },
      // 建立與編輯共用：CreateRequest 只收 categoryId/title/description，
      // 指派人/優先度/日期一律等有了 taskId 後跟編輯流程共用同一段 PUT+PATCH，不重複組裝欄位邏輯
      async saveTask() {
        const form = this.modal.form;
        if (!form.title || !form.title.trim()) {
          this.showToast('標題不可為空');
          return;
        }

        let taskId = this.modal.taskId;
        if (!taskId) {
          const createResult = await api(`/api/projects/${this.projectId}/tasks`, {
            method: 'POST',
            body: JSON.stringify({ categoryId: form.categoryId, title: form.title.trim(), description: form.description || null }),
          });
          if (!createResult.success) {
            this.showToast(createResult.message || '新增失敗');
            return;
          }
          taskId = createResult.data.id;
        }

        const [updateResult, assigneeResult] = await Promise.all([
          api(`/api/projects/${this.projectId}/tasks/${taskId}`, {
            method: 'PUT',
            body: JSON.stringify({
              title: form.title.trim(), description: form.description || null,
              categoryId: form.categoryId, priority: form.priority || null,
              startDate: form.startDate || null, dueDate: form.dueDate || null,
            }),
          }),
          api(`/api/projects/${this.projectId}/tasks/${taskId}/assignee`, {
            method: 'PATCH', body: JSON.stringify({ assigneeId: form.assigneeId }),
          }),
        ]);

        if (updateResult.success && assigneeResult.success) {
          this.modal.open = false;
          await this.loadAll();
        } else {
          this.showToast(updateResult.message || assigneeResult.message || '儲存失敗');
        }
      },
      async deleteTask() {
        if (!confirm('確定刪除此任務？')) return;
        const result = await api(`/api/projects/${this.projectId}/tasks/${this.modal.taskId}`, { method: 'DELETE' });
        if (result.success) {
          this.modal.open = false;
          await this.loadAll();
        } else {
          this.showToast(result.message || '刪除失敗');
        }
      },
    },
  };

```

- [ ] **Step 2: `KanbanView` 引入 mixin，移除已搬移的 data/methods**

`mixins: [toastMixin],` 改成：

```javascript
    mixins: [toastMixin, taskModalMixin],
```

`data()` 裡刪掉 `modal` 這個 key（其餘 `tasks`/`categories`/`members`/`loading`/`columns`/`dragging`/`dragOverCol`/`dragIndex`/`taskWriteQueue`/`categoryPanelOpen`/preset 相關/`editingCategoryId`/`categoryNameDraft`/`draggingCategoryId` 全部保留不動）：

現況：
```javascript
        dragging: null, dragOverCol: null, dragIndex: 0,
        taskWriteQueue: {},  // 同一任務的連續寫入序列化，避免拖曳與 modal 編輯併發時後完成者用舊快照蓋掉新資料（遺失更新）
        modal: {
          open: false, taskId: null,
          form: { title: '', description: '', assigneeId: null, categoryId: null, priority: null, startDate: null, dueDate: null },
        },
        categoryPanelOpen: false,
```

改成：
```javascript
        dragging: null, dragOverCol: null, dragIndex: 0,
        taskWriteQueue: {},  // 同一任務的連續寫入序列化，避免拖曳與 modal 編輯併發時後完成者用舊快照蓋掉新資料（遺失更新）
        categoryPanelOpen: false,
```

`methods` 裡刪掉 `emptyForm`/`openCreate`/`openEdit`/`saveTask`/`deleteTask` 這五個方法（已搬進 mixin，內容跟上面 Step 1 貼的完全一樣）。**注意這五個方法在原檔案裡不是連續的一段**，要分兩處刪除，中間的方法都保留不動：

1. 第一處（原檔案第 94-96 行）：`methods: {` 開頭後緊接著的 `emptyForm() { ... },`，刪掉這一個方法。刪除後 `methods: {` 底下緊接著的就是 `loadAll` 方法（保留不動）。
2. 第二處（原檔案第 189-253 行）：`loadCategoryPresets` 方法之前、`sendMove` 方法之後的 `openCreate`/`openEdit`/`saveTask`/`deleteTask` 四個方法連續區塊，整段刪掉。刪除後 `sendMove` 方法結尾的 `},` 會直接接到 `loadCategoryPresets() {` 開頭。

`loadAll`/`queueTaskWrite`/`tasksIn`/`isOverdue`/`onDragStart`/`onColDragOver`/`onCardDragOver`/`onDrop`/`sendMove` 這些方法都在第一處和第二處之間，維持原樣不動；`loadCategoryPresets` 之後的分類 CRUD 方法也都保留不動。

模板（`template:` 字串）完全不用改——modal 那段 HTML（`<div v-if="modal.open" class="modal-overlay">...`）繼續引用 `modal`/`members`/`categories`/`saveTask`/`deleteTask`/`canWrite`，這些現在由 mixin（`modal`/`saveTask`/`deleteTask`）或 `KanbanView` 自己（`members`/`categories`/`canWrite`）提供，行為完全不變。

- [ ] **Step 3: `mvn test` 確認全綠**

```bash
mvn test
```

Expected: BUILD SUCCESS，測試數與改動前相同（本任務不改後端、不改任何 Java 檔案）。

- [ ] **Step 4: 瀏覽器實測，確認看板 modal 行為沒有因重構而改變**

```bash
docker compose up -d
mvn spring-boot:run
```

以 `leader`/`password123` 登入「MissionBoard 範例專案」，在看板分頁：

1. 點「新增任務」，填標題存檔，確認任務出現在「未開始」欄
2. 點剛新增的任務卡片開 modal，改標題、指派人、所屬類別、優先度、到期日後存檔，確認卡片內容同步更新
3. 再次開啟同一張卡片，點「刪除」，確認卡片從看板消失
4. console 面板確認無錯誤

- [ ] **Step 5: Commit**

```bash
git add src/main/resources/static/js/project-detail.js
git commit -m "refactor: 抽出 taskModalMixin，供看板與後續 WBS 分頁共用任務編輯 modal"
```

---

## Task 2: WBS 檢視分頁（`project-detail.js` + `app.css`）

**Files:**
- Modify: `src/main/resources/static/js/project-detail.js`
- Modify: `src/main/resources/static/css/app.css`

**Interfaces:**
- Consumes: `GET /api/projects/{id}/tasks`、`GET /api/projects/{id}/task-categories`、`GET /api/projects/{id}/members`、`PUT /api/projects/{id}/tasks/{taskId}`（皆為既有、已測試過的端點）；`taskModalMixin` 的 `modal`/`openEdit`/`saveTask`/`deleteTask`（Task 1 產出）
- Produces: 新 module 函式 `buildCategoryTree(categories)`、`sortByDueDate(list)`；新常數 `STATUS_LABELS`；新 mixin `taskBoardMixin`（data: `tasks`/`categories`/`members`/`loading`/`taskWriteQueue`；methods: `loadAll()`/`queueTaskWrite(taskId, task)`）；新元件 `wbs-view`（props：`projectId: Number`、`canWrite: Boolean`）；根 Vue app 新增第三個分頁值 `activeTab === 'wbs'`

### Step 1: 抽出 `buildCategoryTree`／`sortByDueDate`／`STATUS_LABELS`／`taskBoardMixin`（純重構）

`WbsView` 需要看板的「大類→子類」分組邏輯（目前是 `KanbanView.categoryTree` 這個 computed）、人員派工分頁的「依到期日排序」邏輯（目前是 `AssignmentView.tasksFor` 裡內嵌的 comparator）、狀態中文標籤（目前是 `AssignmentView.statusLabel` 裡內嵌的物件字面量），以及看板的 tasks/categories/members 載入＋寫入序列化邏輯。這些如果各自在 `WbsView` 裡重寫一份，會變成三處維護同一份規則，違反專案一貫的抽 mixin 原則（`toastMixin`/`isOverdueDate` 已經是先例）。

- [ ] **Step 1a: 在 `isOverdueDate` 函式之後、`toastMixin` 之前，插入兩個 module-level 函式與一個常數**

```javascript
  // 大類（parentCategoryId 為 null）→ 子類 兩層分組，KanbanView 與 WbsView 共用
  function buildCategoryTree(categories) {
    const stages = categories
      .filter(c => c.parentCategoryId == null)
      .sort((a, b) => a.sortOrder - b.sortOrder);
    return stages.map(stage => ({
      ...stage,
      children: categories
        .filter(c => c.parentCategoryId === stage.id)
        .sort((a, b) => a.sortOrder - b.sortOrder),
    }));
  }

  // 依到期日升冪排序，無到期日排最後；到期日相同（含都無到期日）依 id 升冪，穩定排序不需額外欄位。
  // AssignmentView 與 WbsView 共用同一條規則
  function sortByDueDate(list) {
    return list.sort((a, b) => {
      if (a.dueDate == null && b.dueDate == null) return a.id - b.id;
      if (a.dueDate == null) return 1;
      if (b.dueDate == null) return -1;
      if (a.dueDate !== b.dueDate) return a.dueDate < b.dueDate ? -1 : 1;
      return a.id - b.id;
    });
  }

  const STATUS_LABELS = { NOT_STARTED: '未開始', IN_PROGRESS: '進行中', DONE: '已完成' };

```

- [ ] **Step 1b: 在 `taskModalMixin` 之後、`KanbanView` 定義之前，插入 `taskBoardMixin`**

```javascript
  // KanbanView 與 WbsView 共用：載入 tasks/categories/members 三份資料、loading 狀態，
  // 以及同一任務連續寫入時的序列化佇列（避免拖曳與 modal 編輯併發時後完成者用舊快照蓋掉新資料）
  const taskBoardMixin = {
    data() {
      return { tasks: [], categories: [], members: [], loading: true, taskWriteQueue: {} };
    },
    methods: {
      async loadAll() {
        this.loading = true;
        try {
          const [tasksRes, categoriesRes, membersRes] = await Promise.all([
            api(`/api/projects/${this.projectId}/tasks`),
            api(`/api/projects/${this.projectId}/task-categories`),
            api(`/api/projects/${this.projectId}/members`),
          ]);
          this.tasks = tasksRes.success ? tasksRes.data : [];
          this.categories = categoriesRes.success ? categoriesRes.data : [];
          this.members = membersRes.success ? membersRes.data : [];
          if (!tasksRes.success || !categoriesRes.success || !membersRes.success) {
            this.showToast(tasksRes.message || categoriesRes.message || membersRes.message || '載入失敗，請重新整理');
          }
        } catch (e) {
          this.showToast('載入失敗，請重新整理');
        } finally {
          this.loading = false;
        }
      },
      queueTaskWrite(taskId, task) {
        const prev = this.taskWriteQueue[taskId] || Promise.resolve();
        const next = prev.then(task, task);
        this.taskWriteQueue[taskId] = next;
        return next;
      },
    },
  };

```

- [ ] **Step 1c: `KanbanView` 改用 `taskBoardMixin`**

`mixins: [toastMixin, taskModalMixin],`（Task 1 的產物）改成：

```javascript
    mixins: [toastMixin, taskBoardMixin, taskModalMixin],
```

`data()` 裡刪掉 `tasks: [], categories: [], members: [],`、`loading: true,`、`taskWriteQueue: {},` 這幾行（已搬進 `taskBoardMixin`），保留 `columns`/`dragging`/`dragOverCol`/`dragIndex`/`categoryPanelOpen`/preset 相關/`editingCategoryId`/`categoryNameDraft`/`draggingCategoryId`。

`computed.categoryTree` 現況：
```javascript
      categoryTree() {
        const stages = this.categories
          .filter(c => c.parentCategoryId == null)
          .sort((a, b) => a.sortOrder - b.sortOrder);
        return stages.map(stage => ({
          ...stage,
          children: this.categories
            .filter(c => c.parentCategoryId === stage.id)
            .sort((a, b) => a.sortOrder - b.sortOrder),
        }));
      },
```

改成：
```javascript
      categoryTree() {
        return buildCategoryTree(this.categories);
      },
```

`methods` 裡刪掉 `loadAll`（原檔案第 97-116 行）與 `queueTaskWrite`（原檔案第 117-122 行）這兩個方法（已搬進 mixin，內容跟 Step 1b 貼的完全一樣），`tasksIn`/`isOverdue`/拖曳相關方法/分類 CRUD 方法都保留不動。

- [ ] **Step 1d: `AssignmentView` 改用 `sortByDueDate`／`STATUS_LABELS`（純重構，不改任何行為）**

現況：
```javascript
      statusLabel(status) {
        return { NOT_STARTED: '未開始', IN_PROGRESS: '進行中', DONE: '已完成' }[status];
      },
      // 依到期日升冪排序，無到期日排最後；到期日相同（含都無到期日）依 id 升冪，穩定排序不需額外欄位
      tasksFor(assigneeId) {
        return this.tasks
          .filter(t => t.assigneeId === assigneeId && (this.showDone || t.status !== 'DONE'))
          .sort((a, b) => {
            if (a.dueDate == null && b.dueDate == null) return a.id - b.id;
            if (a.dueDate == null) return 1;
            if (b.dueDate == null) return -1;
            if (a.dueDate !== b.dueDate) return a.dueDate < b.dueDate ? -1 : 1;
            return a.id - b.id;
          });
      },
```

改成：
```javascript
      statusLabel(status) {
        return STATUS_LABELS[status];
      },
      tasksFor(assigneeId) {
        return sortByDueDate(this.tasks.filter(t => t.assigneeId === assigneeId && (this.showDone || t.status !== 'DONE')));
      },
```

`AssignmentView` 不引入 `taskBoardMixin`（它不 fetch `categories`，`loadAll` 內容不同，維持自己原本的 `loadAll` 不動）。

### Step 2: 新增 `WbsView` 元件

在 `AssignmentView` 元件定義結束（`});`）之後、`const app = createApp({` 之前，插入：

```javascript
  const WbsView = defineComponent({
    name: 'WbsView',
    mixins: [toastMixin, taskBoardMixin, taskModalMixin],
    props: {
      projectId: { type: Number, required: true },
      canWrite: { type: Boolean, default: false },
    },
    data() {
      return {
        expandedState: {},
        draggingTask: null,
        dragOverCategoryId: undefined, // undefined=未拖曳中；null=懸停在「未歸類」；number=懸停在該分類節點
      };
    },
    computed: {
      categoryTree() {
        return buildCategoryTree(this.categories);
      },
    },
    methods: {
      isOverdue(t) {
        return isOverdueDate(t);
      },
      statusLabel(status) {
        return STATUS_LABELS[status];
      },
      isExpanded(key) {
        return this.expandedState[key] !== false;
      },
      toggleExpanded(key) {
        this.expandedState[key] = !this.isExpanded(key);
      },
      directTasks(categoryId) {
        return sortByDueDate(this.tasks.filter(t => t.categoryId === categoryId));
      },
      stageIds(stage) {
        return [stage.id, ...stage.children.map(c => c.id)];
      },
      taskCount(ids) {
        return this.tasks.filter(t => ids.includes(t.categoryId)).length;
      },
      completionLabel(ids) {
        const list = this.tasks.filter(t => ids.includes(t.categoryId));
        if (list.length === 0) return '--';
        const done = list.filter(t => t.status === 'DONE').length;
        return `${Math.round(done / list.length * 100)}%`;
      },
      onTaskDragStart(t, ev) {
        if (!this.canWrite) return;
        this.draggingTask = t;
        ev.dataTransfer.effectAllowed = 'move';
      },
      async onCategoryNodeDrop(categoryId) {
        if (!this.draggingTask || !this.canWrite) return;
        const task = this.draggingTask;
        this.draggingTask = null;
        this.dragOverCategoryId = undefined;
        if (task.categoryId === categoryId) return;
        await this.moveTaskToCategory(task, categoryId);
      },
      // 拖曳只改 categoryId，其餘欄位原樣送出（跟 taskModalMixin 的 saveTask() 組 payload 方式相同），
      // 不新增後端端點；走 taskWriteQueue 序列化，避免跟 modal 編輯併發時互相蓋資料
      async moveTaskToCategory(task, categoryId) {
        const prev = task.categoryId;
        task.categoryId = categoryId;
        await this.queueTaskWrite(task.id, async () => {
          const result = await api(`/api/projects/${this.projectId}/tasks/${task.id}`, {
            method: 'PUT',
            body: JSON.stringify({
              title: task.title, description: task.description || null,
              categoryId, priority: task.priority || null,
              startDate: task.startDate || null, dueDate: task.dueDate || null,
            }),
          });
          if (!result.success) {
            task.categoryId = prev;
            this.showToast(result.message || '移動失敗');
          }
        });
      },
    },
    mounted() {
      this.loadAll();
    },
    template: `
      <div>
        <p v-if="loading">載入中...</p>
        <div v-else class="wbs-tree">
          <div class="wbs-node"
               :class="{ 'drag-over': draggingTask && dragOverCategoryId === null }"
               @dragover.prevent="dragOverCategoryId = null" @drop="onCategoryNodeDrop(null)">
            <div class="wbs-node-header" @click="toggleExpanded('unassigned')">
              <span class="wbs-node-toggle">{{ isExpanded('unassigned') ? '▾' : '▸' }}</span>
              <span class="wbs-node-name">未歸類 ({{ taskCount([null]) }})</span>
              <span class="wbs-node-summary">{{ completionLabel([null]) }}</span>
            </div>
            <div v-if="isExpanded('unassigned')" class="wbs-task-list">
              <div v-for="t in directTasks(null)" :key="t.id" class="wbs-task-row"
                   :class="['priority-' + (t.priority || 'NONE')]"
                   :draggable="canWrite" @dragstart="onTaskDragStart(t, $event)" @click="openEdit(t)">
                <span class="wbs-task-title">{{ t.title }}</span>
                <span class="wbs-task-status">{{ statusLabel(t.status) }}</span>
                <span class="wbs-task-assignee">{{ t.assigneeDisplayName || '--' }}</span>
                <span class="wbs-task-due" :class="{ overdue: isOverdue(t) }">{{ t.dueDate || '--' }}</span>
              </div>
            </div>
          </div>

          <div v-for="stage in categoryTree" :key="stage.id" class="wbs-node"
               :class="{ 'drag-over': draggingTask && dragOverCategoryId === stage.id }"
               @dragover.prevent="dragOverCategoryId = stage.id" @drop="onCategoryNodeDrop(stage.id)">
            <div class="wbs-node-header" @click="toggleExpanded(stage.id)">
              <span class="wbs-node-toggle">{{ isExpanded(stage.id) ? '▾' : '▸' }}</span>
              <span class="wbs-node-name">{{ stage.name }} ({{ taskCount(stageIds(stage)) }})</span>
              <span class="wbs-node-summary">{{ completionLabel(stageIds(stage)) }}</span>
            </div>
            <div v-if="isExpanded(stage.id)" class="wbs-node-body">
              <div v-for="t in directTasks(stage.id)" :key="t.id" class="wbs-task-row"
                   :class="['priority-' + (t.priority || 'NONE')]"
                   :draggable="canWrite" @dragstart="onTaskDragStart(t, $event)" @click="openEdit(t)">
                <span class="wbs-task-title">{{ t.title }}</span>
                <span class="wbs-task-status">{{ statusLabel(t.status) }}</span>
                <span class="wbs-task-assignee">{{ t.assigneeDisplayName || '--' }}</span>
                <span class="wbs-task-due" :class="{ overdue: isOverdue(t) }">{{ t.dueDate || '--' }}</span>
              </div>
              <div v-for="child in stage.children" :key="child.id" class="wbs-node wbs-node-child"
                   :class="{ 'drag-over': draggingTask && dragOverCategoryId === child.id }"
                   @dragover.prevent.stop="dragOverCategoryId = child.id" @drop.stop="onCategoryNodeDrop(child.id)">
                <div class="wbs-node-header" @click="toggleExpanded(child.id)">
                  <span class="wbs-node-toggle">{{ isExpanded(child.id) ? '▾' : '▸' }}</span>
                  <span class="wbs-node-name">{{ child.name }} ({{ taskCount([child.id]) }})</span>
                  <span class="wbs-node-summary">{{ completionLabel([child.id]) }}</span>
                </div>
                <div v-if="isExpanded(child.id)" class="wbs-task-list">
                  <div v-for="t in directTasks(child.id)" :key="t.id" class="wbs-task-row"
                       :class="['priority-' + (t.priority || 'NONE')]"
                       :draggable="canWrite" @dragstart="onTaskDragStart(t, $event)" @click="openEdit(t)">
                    <span class="wbs-task-title">{{ t.title }}</span>
                    <span class="wbs-task-status">{{ statusLabel(t.status) }}</span>
                    <span class="wbs-task-assignee">{{ t.assigneeDisplayName || '--' }}</span>
                    <span class="wbs-task-due" :class="{ overdue: isOverdue(t) }">{{ t.dueDate || '--' }}</span>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
        <div v-if="modal.open" class="modal-overlay" @click.self="modal.open = false">
          <div class="modal">
            <h3>{{ modal.taskId ? '編輯任務' : '新增任務' }}</h3>
            <div class="form-group"><label>標題</label><input v-model="modal.form.title" /></div>
            <div class="form-group"><label>描述</label><textarea v-model="modal.form.description" rows="4"></textarea></div>
            <div class="form-group">
              <label>指派人</label>
              <select v-model="modal.form.assigneeId">
                <option :value="null">未指派</option>
                <option v-for="m in members" :key="m.userId" :value="m.userId">{{ m.displayName }}</option>
              </select>
            </div>
            <div class="form-group">
              <label>所屬類別</label>
              <select v-model="modal.form.categoryId">
                <option :value="null">未歸類</option>
                <option v-for="c in categories" :key="c.id" :value="c.id">{{ c.name }}</option>
              </select>
            </div>
            <div class="form-group">
              <label>優先度</label>
              <select v-model="modal.form.priority">
                <option :value="null">未設定</option>
                <option value="HIGH">高</option>
                <option value="MEDIUM">中</option>
                <option value="LOW">低</option>
              </select>
            </div>
            <div class="form-group"><label>起始日</label><input type="date" v-model="modal.form.startDate" /></div>
            <div class="form-group"><label>到期日</label><input type="date" v-model="modal.form.dueDate" /></div>
            <div class="modal-actions">
              <button class="btn btn-primary" @click="saveTask" :disabled="!canWrite">儲存</button>
              <button v-if="modal.taskId && canWrite" class="btn btn-danger" @click="deleteTask">刪除</button>
              <button class="btn" @click="modal.open = false">取消</button>
            </div>
          </div>
        </div>
        <div v-if="toastMessage" class="toast">{{ toastMessage }}</div>
      </div>
    `,
  });

```

`WbsView` 沒有「新增任務」按鈕——本分頁核心只做「移動任務所屬分類」，建立任務維持只在看板做（`openCreate` 雖然透過 mixin 存在，但本分頁模板不會呼叫它）。

### Step 3: 根 app 改三分頁，註冊 `wbs-view`

現況：
```javascript
  const app = createApp({
    data() {
      return { projectId, canWrite, sectionId, activeTab: 'kanban' };
    },
    template: `
      <div>
        <div class="detail-tabs">
          <button class="btn" :class="{ 'btn-primary': activeTab === 'kanban' }" @click="activeTab = 'kanban'">看板</button>
          <button class="btn" :class="{ 'btn-primary': activeTab === 'assignment' }" @click="activeTab = 'assignment'">人員派工</button>
        </div>
        <kanban-view v-if="activeTab === 'kanban'" :project-id="projectId" :can-write="canWrite" :section-id="sectionId" />
        <assignment-view v-else :project-id="projectId" :can-write="canWrite" />
      </div>
    `,
  });

  app.component('kanban-view', KanbanView);
  app.component('assignment-view', AssignmentView);
  app.mount('#detail-app');
})();
```

改成：
```javascript
  const app = createApp({
    data() {
      return { projectId, canWrite, sectionId, activeTab: 'kanban' };
    },
    template: `
      <div>
        <div class="detail-tabs">
          <button class="btn" :class="{ 'btn-primary': activeTab === 'kanban' }" @click="activeTab = 'kanban'">看板</button>
          <button class="btn" :class="{ 'btn-primary': activeTab === 'assignment' }" @click="activeTab = 'assignment'">人員派工</button>
          <button class="btn" :class="{ 'btn-primary': activeTab === 'wbs' }" @click="activeTab = 'wbs'">WBS 檢視</button>
        </div>
        <kanban-view v-if="activeTab === 'kanban'" :project-id="projectId" :can-write="canWrite" :section-id="sectionId" />
        <assignment-view v-else-if="activeTab === 'assignment'" :project-id="projectId" :can-write="canWrite" />
        <wbs-view v-else :project-id="projectId" :can-write="canWrite" />
      </div>
    `,
  });

  app.component('kanban-view', KanbanView);
  app.component('assignment-view', AssignmentView);
  app.component('wbs-view', WbsView);
  app.mount('#detail-app');
})();
```

### Step 4: `app.css` 加 WBS 樹狀樣式

在 `app.css` 檔尾（既有 `.assignment-col` 規則之後）加：

```css
.wbs-tree { display: flex; flex-direction: column; gap: 0.4rem; }
.wbs-node { background: #fff; border: 1px solid #dfe6e9; border-radius: 6px; padding: 0.5rem 0.75rem; }
.wbs-node.drag-over { outline: 2px dashed #0984e3; outline-offset: -4px; }
.wbs-node-header { display: flex; align-items: center; gap: 0.5rem; cursor: pointer; font-weight: 600; padding: 0.25rem 0; }
.wbs-node-toggle { width: 1rem; color: #636e72; }
.wbs-node-name { flex: 1; }
.wbs-node-summary { font-size: 0.85rem; color: #636e72; }
.wbs-node-body { margin-left: 1.5rem; margin-top: 0.4rem; display: flex; flex-direction: column; gap: 0.4rem; }
.wbs-node-child { margin-top: 0.4rem; }
.wbs-task-list { margin-left: 1.5rem; margin-top: 0.3rem; display: flex; flex-direction: column; gap: 0.3rem; }
.wbs-task-row { display: flex; align-items: center; gap: 0.75rem; padding: 0.4rem 0.6rem; border-radius: 4px; background: #f8f9fa; border-left: 4px solid #b2bec3; cursor: grab; font-size: 0.88rem; }
.wbs-task-row:active { cursor: grabbing; }
.wbs-task-row.priority-HIGH { border-left-color: #d63031; }
.wbs-task-row.priority-MEDIUM { border-left-color: #e17055; }
.wbs-task-row.priority-LOW { border-left-color: #00b894; }
.wbs-task-title { flex: 1; font-weight: 500; }
.wbs-task-status { color: #636e72; }
.wbs-task-assignee { color: #636e72; }
.wbs-task-due.overdue { color: #d63031; font-weight: 700; }
```

### Step 5: `mvn test` 確認全綠

```bash
mvn test
```

Expected: BUILD SUCCESS，測試數與改動前相同（本任務不改後端）。

### Step 6: 瀏覽器實測

```bash
docker compose up -d
mvn spring-boot:run
```

以 `leader`/`password123` 登入「MissionBoard 範例專案」（種子資料：大類 `SIT` → 子類 `程式開發`；任務「設計登入頁」歸在程式開發／`DONE`／指派 member，「實作看板拖曳」歸在程式開發／`IN_PROGRESS`／指派 leader，「整理需求訪談紀錄」未歸類／`NOT_STARTED`／未指派），依序確認：

1. 專案詳情頁上方出現「看板」「人員派工」「WBS 檢視」三顆分頁按鈕
2. 點「WBS 檢視」，看到「未歸類 (1)」節點（`--` 完成度，因為 0 筆已完成）與「SIT (2) 40%」→ 展開後看到子節點「程式開發 (2) 50%」，其下兩張任務列（「設計登入頁」標「已完成」、「實作看板拖曳」標「進行中」）
3. 點節點的展開/收合箭頭，確認可收合再展開
4. 把「整理需求訪談紀錄」從「未歸類」拖曳到「SIT」節點上，確認任務列移過去、「未歸類」變成 `(0)`、「SIT」變成 `(3)`；重新整理頁面後仍在新分類下（後端已寫入）
5. 切回「看板」分頁，確認該任務卡片的所屬分類已同步顯示為「SIT」（驗證 `v-if` 重新 fetch 沒有拿到舊資料）
6. 切回「WBS 檢視」，點任一任務列開 modal，確認跟看板點卡片開的是同一顆 modal（標題/描述/指派人/所屬類別/優先度/日期都在），改「所屬類別」存檔後樹狀資料同步更新
7. 用 `member`（`PROJECT_MEMBER`，非本專案 leader）或封存後的專案登入測試唯讀情境：任務列不可拖曳（`draggable` 應為 `false`）
8. 快速切「看板」→「人員派工」，確認人員派工分頁排序與狀態標籤顯示正常（Step 1d 重構後的回歸檢查）
9. console 面板確認無錯誤
10. 依專案 CLAUDE.md「有畫面就有截圖」規則截圖存證（WBS 檢視初始展開狀態、拖曳後狀態，兩張以內）

### Step 7: Commit

```bash
git add src/main/resources/static/js/project-detail.js src/main/resources/static/css/app.css
git commit -m "feat: 新增 WBS 檢視分頁，可拖曳任務改變所屬大類/子類"
```

---

## Task 3: 更新 CLAUDE.md 反映三分頁現況

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: 修正「專案狀態」段落（第 9 行）**

現況：
```
`tasks`/`task_categories`/`task_category_presets` 已取代舊的 `wbs_nodes`/`wbs_presets`，`wbs` 套件與相關 DDL 已移除。專案詳情頁為雙分頁：看板（`KanbanView`，預設分頁）可拖曳卡片跨欄、建立任務、歸類、指派；人員派工（`AssignmentView`）依成員分欄，可拖曳卡片跨欄改指派人，皆走 REST＋樂觀更新。舊的樹編輯器／人員派工／甘特三個分頁曾隨重構移除，人員派工已依新的扁平任務模型重做完成；樹編輯器／甘特若後續要重做，需另行設計，不可沿用舊 `wbs_nodes` 邏輯。依任務路由表，新功能一律先走 `superpowers:brainstorming`。
```

改成：
```
`tasks`/`task_categories`/`task_category_presets` 已取代舊的 `wbs_nodes`/`wbs_presets`，`wbs` 套件與相關 DDL 已移除。專案詳情頁為三分頁：看板（`KanbanView`，預設分頁）可拖曳卡片跨欄、建立任務、歸類、指派；人員派工（`AssignmentView`）依成員分欄，可拖曳卡片跨欄改指派人；WBS 檢視（`WbsView`）依兩層分類（大類/子類）呈現樹狀結構＋固定的「未歸類」節點，可拖曳任務改變所屬大類/子類，皆走 REST＋樂觀更新。舊的樹編輯器／人員派工／甘特三個分頁曾隨重構移除，人員派工與 WBS 檢視已依新的扁平任務模型重做完成，僅剩甘特分頁待補；若後續要重做，需另行設計，不可沿用舊 `wbs_nodes` 邏輯。依任務路由表，新功能一律先走 `superpowers:brainstorming`。
```

- [ ] **Step 2: 修正「前端模式」段落（第 73 行）**

現況：
```
專案詳情頁（`project-detail.js`）為雙分頁，各自獨立載入資料、以 `v-if` 切換（非 `v-show`，避免兩分頁資料不同步）：看板（`KanbanView`，預設分頁）一次載入任務與分類資料；人員派工（`AssignmentView`）載入任務與成員資料，依成員分欄（含「未指派」欄）呈現，v1 僅支援拖曳改指派，不支援點卡片開 modal（完整編輯回看板做）。兩分頁共用 `toastMixin`（提示訊息與計時器），避免重複邏輯。所有修改走 REST，成功後就地更新（樂觀更新＋失敗回滾、fetch 失敗顯示 toast）：看板拖曳卡片跨欄呼叫 `move` 端點、建立任務預設「未歸類」（`category_id` 為 NULL）、點卡片開 modal 編輯歸類／指派／優先度／日期；人員派工拖曳卡片跨欄呼叫 `assignee` 端點，欄內排序為固定規則（依到期日，無到期日排最後）。同一任務的連續寫入經 `taskWriteQueue` 序列化，避免拖曳與 modal 編輯併發時後完成者用舊快照蓋掉新資料。
```

改成：
```
專案詳情頁（`project-detail.js`）為三分頁，各自獨立載入資料、以 `v-if` 切換（非 `v-show`，避免分頁間資料不同步）：看板（`KanbanView`，預設分頁）一次載入任務與分類資料；人員派工（`AssignmentView`）載入任務與成員資料，依成員分欄（含「未指派」欄）呈現，v1 僅支援拖曳改指派，不支援點卡片開 modal（完整編輯回看板做）；WBS 檢視（`WbsView`）依兩層分類呈現大類→子類→任務的樹狀結構＋固定的「未歸類」節點，節點旁顯示任務數與完成度（前端即時算），核心互動是拖曳任務改變所屬大類/子類，點任務列開與看板相同的編輯 modal，分類本身的新增/改名/刪除/排序仍只在看板的「分類管理」面板操作。看板與 WBS 檢視共用 `taskBoardMixin`（載入 tasks/categories/members 三份資料與 `taskWriteQueue` 序列化寫入佇列）與 `taskModalMixin`（新增/編輯/刪除任務的 modal 邏輯），三分頁共用 `toastMixin`（提示訊息與計時器），避免重複邏輯。所有修改走 REST，成功後就地更新（樂觀更新＋失敗回滾、fetch 失敗顯示 toast）：看板拖曳卡片跨欄呼叫 `move` 端點、建立任務預設「未歸類」（`category_id` 為 NULL）、點卡片開 modal 編輯歸類／指派／優先度／日期；人員派工拖曳卡片跨欄呼叫 `assignee` 端點，欄內排序為固定規則（依到期日，無到期日排最後）；WBS 檢視拖曳任務改分類重用既有任務 `PUT` 端點（帶入原欄位＋新 `categoryId`），不新增後端端點。同一任務的連續寫入經 `taskWriteQueue` 序列化，避免拖曳與 modal 編輯併發時後完成者用舊快照蓋掉新資料。
```

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: CLAUDE.md 反映 WBS 檢視分頁已依新模型重做完成"
```

---

## 收尾：驗證

- [ ] Task 1 Step 4、Task 2 Step 6 的瀏覽器實測全數通過、Task 3 的文件更新完成即整體完成，無需額外收尾步驟。
