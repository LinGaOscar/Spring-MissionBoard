# 使用說明

本文件說明 Spring-MissionBoard 的角色權限與各項可用功能，適合系統使用者（非開發者）閱讀。開發環境建置請見 [dev.md](dev.md)。

## 登入

- 開啟系統後導向 `/login`，以帳號密碼表單登入。
- 登入成功導向首頁（`/home`）的儀表板，內容依角色不同（見下方「首頁儀表板」）；實際操作由「專案列表」或儀表板上的專案連結進入。
- 未登入時，一般頁面會被導回登入頁；API 請求則回傳 401。

## 角色與權限

系統有四種角色，權限範圍由高至低如下：

| 角色 | 顯示名稱 | 可讀範圍 | 可寫範圍 | 可封存／還原專案 |
|---|---|---|---|---|
| `DIRECTOR` | 主任 | 全部科別的所有專案（跨科） | 一律唯讀，不可寫入任何專案 | 不可 |
| `SECTION_CHIEF` | 科長 | 僅同科別的所有專案 | 同科別且**未封存**的專案 | 可（同科別，不受封存狀態限制） |
| `PROJECT_LEADER` | 專案負責人 | 僅自己參與（成員）的專案 | 自己參與且**未封存**的專案 | 可（僅限自己參與的專案） |
| `PROJECT_MEMBER` | 專案成員 | 僅自己參與（成員）的專案 | 自己參與且**未封存**的專案 | 可（僅限自己參與的專案） |

補充說明：

- **封存專案＝全員唯讀**：專案一旦封存，即使是科長或負責人也無法修改任務、分類或成員，只能檢視；「封存」動作本身刻意不受此限制，否則封存後會沒有人能還原。主任始終唯讀，不能封存或還原任何專案。
- **科別隔離**：專案列表依角色不同而不同——主任看到全部；科長看到本科全部專案；專案負責人／成員只看到自己被加入的專案，不受科別限制（即使被拉進其他科的專案也看得到）。
- 目前 `PROJECT_LEADER` 與 `PROJECT_MEMBER` 在系統權限判斷上完全相同，沒有額外差異化的操作。

### 測試／示範帳號

以下帳號僅供本機測試環境使用，密碼皆為 `password123`：

| 帳號 | 角色 | 所屬科別 | 參與專案 |
|---|---|---|---|
| `director` | 主任 | 資訊部（部級） | 無直接參與，可跨科唯讀所有專案 |
| `chief` | 科長 | 系統科 | 無直接參與，可讀寫系統科所有專案 |
| `leader` | 專案負責人 | 系統科 | 「MissionBoard 範例專案」負責人兼成員 |
| `member` | 專案成員 | 系統科 | 「MissionBoard 範例專案」成員 |
| `member2` | 專案成員二 | 網路科 | 未加入任何專案，用於驗證跨科看不到系統科專案 |

## 主要功能

### 首頁儀表板

登入後的首頁依角色顯示三種視角之一：

- **個人視角**（專案負責人、專案成員）：我參與的進行中專案卡片，以及指派給我的任務列表。
- **管理視角**（科長）：本科所有未封存專案的彙總列（不限自己是否為成員），每列可點擊進入該專案詳情頁。
- **總覽視角**（主任）：跨科彙總，依科別分組顯示專案數與任務統計；列不可點擊。

### 專案列表

- 進入「專案列表」頁會列出目前使用者可讀的專案卡片，每張卡片顯示專案名稱、所屬科別與負責人；頁面上方可切換「未封存／已封存」頁籤。
- 點卡片進入該專案的詳情頁。
- 點「新增專案」按鈕開啟表單，填寫名稱（必填）與描述後建立，所屬科別與負責人自動代入建立者本人與部門，建立成功後導向新專案的詳情頁。

### 專案詳情頁

進入專案後，左側側邊欄的專案名稱底下會出現三個分頁連結，點擊切換：

#### 1. 看板（預設分頁）

- 任務依狀態分「未開始／進行中／已完成」三欄呈現，可拖曳卡片跨欄改變狀態，或在欄內拖曳調整順序。
- 有寫入權限時可點「新增任務」開啟編輯視窗建立任務，新任務預設「未歸類」。
- 點任一卡片可開啟編輯視窗，修改標題、描述、指派人、所屬分類、優先度、起訖日，或刪除任務。
- 逾期任務（到期日已過且尚未完成）以紅色標示；已完成的任務不會被標示逾期。
- 看板不管理分類；大類／子類別的建立與調整請到「WBS 檢視」分頁操作。

#### 2. 人員派工

- 以專案成員為欄（含「未指派」欄）呈現任務卡片，可拖曳卡片跨欄改變指派人。
- 可用「顯示已完成」開關切換是否顯示已完成任務（預設隱藏）。
- 欄內任務依到期日排序，無到期日的排最後。
- 此分頁僅支援拖曳改指派，不支援點卡片開編輯視窗；完整編輯請回看板分頁操作。

#### 3. WBS 檢視

- 以「大類 → 子類別 → 任務」樹狀結構呈現，並固定顯示「未歸類」節點。
- 每個節點旁顯示任務數與完成度。
- 可拖曳任務到不同的分類節點，改變該任務所屬的大類／子類。
- 點任務列會開啟與看板相同的編輯視窗；任務列右側的狀態文字在有寫入權限時可點擊，在「未開始→進行中→已完成」間循環切換。
- 「未歸類」節點與每個子類別節點旁有「+ 新增」輸入框，只填標題即可快速建立任務，直接歸入該節點。
- **分類管理**（全站只在此分頁操作）：
  - 樹狀結構最底部有一排大項切換鈕：尚未啟用的大項顯示「啟用」，點擊即從預設選單建立；已啟用的顯示「已啟用」，再點一次即停用（連同其下子類別一併刪除，任務自動變回「未歸類」，不會被刪除）。大項標頭旁另有「刪除」按鈕，效果相同。
  - 每個大項旁「+ 新增子項」：直接輸入名稱建立子類別，不需從選單挑選。
  - 雙擊大項或子類別名稱可就地改名。
  - 拖曳節點左側「⠿」把手可調整同層排序；子類別也可拖到別的大項底下（放到大項標題列上補到最後、放到某個子類別上則插入該位置）。大項本身不可拖到其他大項底下（分類最多兩層）。
- 工具列有「匯出 Excel」按鈕，下載內容涵蓋整個專案的任務清單，欄位為大類／子類／任務標題／指派人／狀態／優先度／起始日／到期日，列的排序對齊畫面上的樹狀顯示順序；任何有讀取權限的角色（含封存專案）都能匯出。

### 任務欄位

任務可編輯的欄位：標題（必填）、描述、指派人（限專案成員）、所屬分類（未歸類或任一分類）、優先度（高／中／低／未設定）、起始日、到期日、狀態。

### 專案層級操作（詳情頁工具列）

- **封存／解封存**：`canArchive` 為真的角色（科長、負責人、專案成員；主任恆不可）才會看到按鈕。封存後專案變成全員唯讀，工具列按鈕會變成「解封存」。
- **成員管理**：點「成員管理」展開面板，可查看目前成員（負責人特別標示）；有寫入權限者可新增成員（下拉列出全部使用者，不限科別）、移除成員（現任負責人無法被移除，需先換負責人）、更換負責人。

## 相關文件

- 開發環境建置與常用指令：[dev.md](dev.md)
- 架構決策與設計脈絡：`docs/superpowers/specs/`
